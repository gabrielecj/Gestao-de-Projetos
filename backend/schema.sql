CREATE TABLE IF NOT EXISTS entries(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,type TEXT NOT NULL,age_group TEXT NOT NULL,sex TEXT NOT NULL,sector TEXT NOT NULL,date TEXT NOT NULL,entry_time TEXT NOT NULL,exit_time TEXT);